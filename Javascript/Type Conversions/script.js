// String Conversion
let bool = true; // boolean
let stringBool = String(bool);//string "true"
alert(typeof stringBool)

// Numeric conversion
let userName = prompt("What is your name:");
let userAge = Number(userName);
alert(userAge);

// Boolean Conversion
let name = "0";
name = Boolean("0");
alert(name);
//---------------
let menu = 0;
menu = Boolean(0);
alert(menu);

