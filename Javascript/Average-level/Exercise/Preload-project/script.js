const divLoader = document.querySelector(".loader");

window.addEventListener("load", function () {
  //   divLoader.classList.add("hidden");
  // Or :

  divLoader.className += " hidden"; // class="loader hidden"
});
