// For every loop iteration, write down which value it outputs and then compare it with the solution.

// Both loops alert the same values, or not?

// return from 1 to 4
// var i = 0;
// while (++i < 5) alert(i);

// return from 1 to 5 => (old value)
var i = 0;
while (i++ < 5) alert(i);
/*

(0 < 5) alert( 1 );
(1 < 5) alert( 2 );
(2 < 5) alert( 3 );
(3 < 5) alert( 4 );
(4 < 5) alert( 5 );

*/
